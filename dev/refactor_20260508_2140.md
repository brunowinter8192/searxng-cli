# Refactor Scan — src/ — 2026-05-08 21:40

Source root: `src/` (4065 LOC across 30 Python files)
Standards: `~/.claude/shared-rules/worker/code-standards.md`, `code-organization.md`
Project notes: `src/DOCS.md` documents 2 root-level files (`routing.py` entry-point, `spawn/tmux_spawn.sh` shell library — both justified)

## Documented Exceptions

- `src/routing.py` — entry-point for `cli.py`, justified as root-level standalone.
- `src/spawn/tmux_spawn.sh` — single-file subdir, documented in parent `src/DOCS.md` per documentation rules.
- Module-level state in `src/search/rate_limiter.py` (`_limiters` dict) — owned by one module, accessed via `get_limiter()` accessor. Compliant with code-standards.md Module-Level Mutable State rule.

## Phase 2 — Scan Output

### 2.1 File-LOC

| Severity | File | LOC |
|---|---|---|
| WATCH | `src/search/search_web.py` | 394 |
| WATCH | `src/crawler/crawl_site.py` | 308 |

Zero HARD violations (>400). Both watch-list files have a structural split candidate — see Phase 3.

### 2.2 Function-LOC

| LOC | Function | File | Line |
|---|---|---|---|
| 125 | `search_web_workflow` | search_web.py | 102 |
| 69 | `explore_site_workflow` | explore_site.py | 18 |
| 68 | `_merge_and_rank` | merge.py | 57 |
| 66 | `download_pdf_workflow` | download_pdf.py | 26 |
| 56 | `scrape_url_raw_workflow` | scrape_url_raw.py | 22 |
| 56 | `_engine_with_timing` | search_web.py | 319 |
| 53 | `crawl_site_workflow` | crawl_site.py | 25 |
| 51 | `discover_urls` | crawl_site.py | 104 |

One HARD violation (≥100 LOC): `search_web_workflow`. Seven WATCH (50-99 LOC) — most are `*_workflow` orchestrators.

### 2.3 Argument-Mutation

Total hits: **0**. Codebase clean on Immutability rule.

### 2.4 Root-Module Justification

`src/routing.py` — 1 subdir use + entry-point reference in `cli.py`. JUSTIFIED.
No relocations.

### 2.5 Coupling

>5 imports per file: 12 files. Note: scan counts stdlib (logging, asyncio, os, etc.) — real cross-module coupling is materially lower. After excluding stdlib, no file exceeds 4-5 internal/3rd-party imports. Heuristic flags noisy here; no real over-coupling.

## Phase 3 — Prioritized Candidates

| Severity | Target | Issue | Recommendation |
|---|---|---|---|
| HARD | `search_web.py :: search_web_workflow` (125 LOC) | Orchestrator violates "calls only / zero functional logic" rule from code-organization.md — has inline engine-fanout accumulator loops, query-log-entry dict construction, filter-mode branches | Extract three helpers: `_apply_filter_modes(selected, books, pdf, docs)`, `_run_engine_fanout(selected, query, language, with_timings, ...)`, `_build_query_log_entry(query, language, engines, total_ms, engine_stats, preview_stats)`. Reduces orchestrator to ~50 LOC of pure composition. |
| WATCH-FILE | `search_web.py` (394 LOC) | Approaching 400 ceiling | Solved by HARD refactor above + extracting `_BOOKS_*`, `_PDF_*`, `_DOCS_*` constants/maps into `src/search/filter_modes.py`. Net LOC reduction ~80, pulls file comfortably below 320. |
| WATCH-FILE | `crawl_site.py` (308 LOC) | Approaching 300 watch threshold | Lower priority. `discover_urls` (51 LOC) is one extractable helper but no clear concern split inside the file. Heuristic interpretation: leave at N+ε unless a structural split emerges. **No action recommended.** |
| WATCH-FN | 6× `*_workflow` functions (53-69 LOC) in explore_site, download_pdf, scrape_url_raw, crawl_site | All are workflow orchestrators with embedded inline logic | Per file: extract 1-2 helpers each to bring orchestrators to pure composition. Lower priority than search_web — these don't violate hard ceilings. **Bundle as a follow-up "workflow-orchestrator-cleanup" task.** |
| WATCH-FN | `_merge_and_rank` (68 LOC) in merge.py | Logic function, not workflow — 50-99 range | Inspect for extraction candidates, but logic-functions can legitimately be longer than workflows. Likely leave as-is unless a split improves readability. |
| WATCH-FN | `_engine_with_timing` (56 LOC) in search_web.py | Try/except cascade with 6 except branches mapping exceptions to status codes | Necessarily verbose — try/except chains don't extract well into helpers. Per heuristic interpretation: **leave as-is**. |
| STRUCT-DOC | `src/DOCS.md` | Stale: claims "8 active engines" + "Google Scholar via HTTP" — actual is 10 engines + Scholar pydoll-browser | One-paragraph fix in `src/DOCS.md`. |

## Phase 4 — Refactor Plan

Three concrete worker-dispatchable units, ordered by impact:

### Unit A — search_web.py orchestrator + filter-mode extraction (HARD + WATCH-FILE combined)

**Worker name:** `search-web-orchestrator-cleanup`
**Scope:**
1. Extract `_apply_filter_modes` helper from `search_web_workflow` (lines 117-136 logic).
2. Extract `_run_engine_fanout` helper from `search_web_workflow` (lines 144-163 logic).
3. Extract `_build_query_log_entry` helper from `search_web_workflow` (lines 199-211 logic).
4. Move `_BOOKS_ENGINES` / `_BOOKS_MODIFIER` / `_PDF_ENGINES` / `_PDF_MODIFIER` / `_DOCS_ENGINES` / `_DOCS_MODIFIER` constants + their selection logic into new module `src/search/filter_modes.py`.
5. Update imports in `search_web.py` to consume the new module.
6. Verify no behavior change via existing dev/search_pipeline smokes (single-query + a small batch).

**Reference patterns:** `src/search/snippet.py` and `src/search/merge.py` for "logic extracted into single-responsibility module" pattern. `src/search/cache.py` for "module-level constants + few public functions" structure.

**Import-update impact:** `search_web.py` only is the consumer. `cli.py` calls the workflow function — name unchanged, no caller updates.

**Estimated LOC after refactor:** search_web.py ~310, new filter_modes.py ~50.

### Unit B — DOCS.md sync (STRUCT-DOC)

**Worker name:** `src-docs-sync` (or fold into Unit A — minor)
**Scope:** Update `src/DOCS.md` engine count (8 → 10) and Scholar architecture line ("Google Scholar via HTTP" → "Google Scholar via pydoll browser"). Also update the Documentation Tree sub-line under `search/` if needed.

Trivial 5-LOC change. Could be folded into Unit A's commit if scope-creep is small.

### Unit C — workflow-orchestrator cleanup (WATCH-FN, lower priority)

**Worker name:** `workflow-orchestrator-cleanup`
**Scope:** explore_site.py, download_pdf.py, scrape_url_raw.py, crawl_site.py — extract 1-2 helpers each to bring `*_workflow` functions to pure composition (≤30 LOC each, calls only).

**Defer until Unit A is merged and validated.** Lower urgency, four files instead of one, no hard ceiling violations.

### Not refactoring (deliberate)

- `_merge_and_rank` (68 LOC) — logic function, length is justified by single concern (slot allocation across classes).
- `_engine_with_timing` (56 LOC) — try/except cascade, no clean extraction.
- `crawl_site.py` (308 LOC) — at watch threshold but no clear concern split.
- High-import files — coupling scan inflated by stdlib counts; no real over-coupling.

## Summary

Codebase is in good structural shape. One hard refactor target (`search_web_workflow`), one stale doc paragraph, and a tier of optional workflow-cleanups. Total real refactor work: **one focused worker (Unit A) + one trivial doc fix**. Unit C deferrable.
